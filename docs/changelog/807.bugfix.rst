Stop :func:`~turbohtml.clean.sanitize` escape mode from rendering a start tag for an element the parser inserted, such
as the ``tbody`` a bare ``<table><tr>`` implies, and escape a stray ``</p>`` or ``</br>`` as written.
