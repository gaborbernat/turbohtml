:class:`turbohtml.IncrementalParser` honors a byte-order mark at the start of bytes input: the mark picks the
encoding over the ``encoding`` argument and is stripped, as :func:`turbohtml.parse` does.
